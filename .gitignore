# Node
node_modules
dist
*.log

# Rust
target/
Cargo.lock

# Tauri
src-tauri/target

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Lockfiles should be committed for reproducible builds
# pnpm-lock.yaml 已移除，lockfile 应该被提交到版本控制
# package-lock.json 已移除，lockfile 应该被提交到版本控制
/tsconfig.node.tsbuildinfo